# Example 2.19

#include <stdlib.h>

void freemakeargv (char **argv) {

   if (*argv == NULL)
      return;
   if (*argv != NULL)
      free (*argv); /* causes the memory allocated for all strings to be freed *.
   free (argv);
}

# Example 2.24
   ...
   if (makeargv (path, PATH_DELIMITERS, &myargv) == -1)
      return NULL;
   else
      return myargv;
}
